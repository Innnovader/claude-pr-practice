# Supabase (claves públicas — la service_role key NUNCA va en el frontend,
# esa vive solo en services/scraper/.env)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Mapbox (opcional — solo si se usa Mapbox GL en vez del basemap gratuito de Leaflet/OSM)
NEXT_PUBLIC_MAPBOX_TOKEN=
